"use client";

import Link from "next/link";
import { useMemo, useState } from "react";

type Rates = Record<string, number>;
type Props = { packageTitle?: string; packageId?: string; groupRates?: Rates };
const blocked:Record<string,string>={"2026-10-20":"Dussehra","2026-11-08":"Diwali","2026-11-24":"Dev Deepawali","2026-12-25":"Christmas","2026-12-31":"New Year's Eve","2027-01-01":"New Year","2027-01-26":"Republic Day"};
const iso=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;
const addDays=(n:number)=>{const d=new Date();d.setHours(12,0,0,0);d.setDate(d.getDate()+n);return iso(d)};
function rateForPax(r:Rates|undefined,p:number){if(!r)return 0;if(p<=2)return r["2"]??0;if(p<=4)return r["4"]??r["2"]??0;if(p<=6)return r["6"]??r["4"]??0;if(p<=12)return r["12"]??r["6"]??0;if(p<=16)return r["16"]??r["12"]??0;if(p<=20)return r["20"]??r["16"]??0;if(p<=25)return r["25"]??r["20"]??0;if(p<=30)return r["30"]??r["25"]??0;return r["30+"]??r["30"]??0}
export default function BookingForm({packageTitle,packageId,groupRates}:Props){
 if(!packageTitle||!groupRates)return null;
 const[name,setName]=useState(""),[phone,setPhone]=useState(""),[email,setEmail]=useState(""),[travelDate,setTravelDate]=useState(""),[adults,setAdults]=useState(2),[children,setChildren]=useState(0),[nationality,setNationality]=useState("Indian"),[dob,setDob]=useState(""),[idType,setIdType]=useState("Aadhaar"),[idNumber,setIdNumber]=useState(""),[message,setMessage]=useState(""),[room,setRoom]=useState("double"),[accepted,setAccepted]=useState(false);
 const totalPax=Math.max(1,adults+children),basePP=rateForPax(groupRates,totalPax),singlePP=Math.ceil(basePP*1.38/500)*500;
 const roomOptions=useMemo(()=>{const rooms=Math.ceil(totalPax/2);return[{id:"double",name:`${rooms} Double Room${rooms>1?"s":""} with Double Bed`,price:basePP*totalPax},{id:"single",name:`${totalPax} Single Room${totalPax>1?"s":""}`,price:singlePP*totalPax},{id:"twin",name:`${rooms} Twin Room${rooms>1?"s":""} with Twin Bed`,price:basePP*totalPax}]},[totalPax,basePP,singlePP]);
 const selectedRoom=roomOptions.find(x=>x.id===room)??roomOptions[0],festival=blocked[travelDate],minBookDate=addDays(7);
 const selectedLead=useMemo(()=>travelDate?Math.ceil((new Date(`${travelDate}T12:00:00`).getTime()-new Date().setHours(12,0,0,0))/86400000):999,[travelDate]),enquiryOnly=selectedLead>=0&&selectedLead<7;
 function submit(e:React.FormEvent){e.preventDefault();if(!accepted){alert("Please accept the Terms & Conditions and Cancellation Policy before continuing.");return}alert(festival||enquiryOnly?"Enquiry submitted. Our team will confirm urgent/special-date availability.":"Booking request submitted successfully. Our team will confirm availability and payment details.")}
 return <section id="booking" className="scroll-mt-28 rounded-3xl bg-white p-6 shadow-xl md:p-8"><h2 className="text-3xl font-bold">Book This Tour</h2><p className="mt-2 font-medium text-slate-600">{packageTitle}{packageId?` • ${packageId}`:""}</p><div className="mt-5 rounded-xl border border-blue-100 bg-blue-50 p-4 text-sm"><b>Online booking:</b> minimum 7 days advance. Travel within 1–6 days and blocked festival dates are enquiry-only.</div><div className="mt-4 flex flex-wrap gap-2 text-xs">{Object.entries(blocked).map(([d,l])=><span key={d} className="rounded-full border border-rose-200 bg-rose-50 px-3 py-1.5 text-rose-700">● {l} • {new Date(`${d}T12:00:00`).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"})}</span>)}</div><form onSubmit={submit} className="mt-7 space-y-5"><div className="grid gap-5 md:grid-cols-2"><Field label="Full Name"><input required value={name} onChange={e=>setName(e.target.value)} className="input"/></Field><Field label="Mobile Number"><input required type="tel" value={phone} onChange={e=>setPhone(e.target.value)} className="input"/></Field><Field label="Email Address"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="input"/></Field><Field label="Nationality"><input required value={nationality} onChange={e=>setNationality(e.target.value)} className="input"/></Field><Field label="Date of Birth"><input required type="date" max={addDays(0)} value={dob} onChange={e=>setDob(e.target.value)} className="input"/></Field><Field label="Travel Date"><input required type="date" min={addDays(1)} value={travelDate} onChange={e=>setTravelDate(e.target.value)} className="input"/><p className={`mt-2 text-xs ${festival||enquiryOnly?"text-rose-600":"text-slate-500"}`}>{festival?`● ${festival} — booking closed; enquiry only.`:enquiryOnly?"● Within 7 days — urgent enquiry only.":`Online booking available from ${new Date(`${minBookDate}T12:00:00`).toLocaleDateString("en-IN")}.`}</p></Field><Field label="ID Type"><select value={idType} onChange={e=>setIdType(e.target.value)} className="input"><option>Aadhaar</option><option>PAN</option><option>Passport</option><option>Other Government ID</option></select></Field><Field label={`${idType} Number`}><input required value={idNumber} onChange={e=>setIdNumber(e.target.value)} className="input"/></Field><Field label="Adults"><input type="number" min={1} value={adults} onChange={e=>setAdults(Number(e.target.value))} className="input"/></Field><Field label="Children"><input type="number" min={0} value={children} onChange={e=>setChildren(Number(e.target.value))} className="input"/></Field></div><div className="rounded-2xl border border-slate-200 p-5"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-red-500 text-xl font-extrabold text-white">3</span><h3 className="text-2xl font-extrabold">Select Accommodation</h3></div><p className="mt-3 text-slate-600">Select your preferred room type. Pricing updates with the number of travellers.</p><div className="mt-5 overflow-x-auto"><table className="w-full border-collapse text-left"><thead><tr className="bg-slate-50"><th className="border p-3">Room Type</th><th className="border p-3">Package Total</th><th className="border p-3 text-center">Selection</th></tr></thead><tbody>{roomOptions.map(o=><tr key={o.id} className={room===o.id?"bg-blue-50":""}><td className="border p-3 font-bold">{o.name}</td><td className="border p-3 font-extrabold">₹{o.price.toLocaleString("en-IN")}</td><td className="border p-3 text-center"><input type="radio" name="room" value={o.id} checked={room===o.id} onChange={()=>setRoom(o.id)} className="h-5 w-5 accent-blue-700" aria-label={`Select ${o.name}`}/></td></tr>)}</tbody></table></div><p className="mt-3 text-xs text-slate-500">Single-room option includes an estimated single occupancy supplement. Final hotel availability and room configuration are confirmed before payment.</p></div>{basePP>0&&<div className="rounded-2xl bg-slate-950 p-5 text-white"><div className="flex justify-between gap-4"><span>{selectedRoom.name}</span><b>₹{selectedRoom.price.toLocaleString("en-IN")}</b></div><div className="mt-3 flex justify-between border-t border-white/20 pt-3"><span>Estimated Package Total</span><b className="text-xl text-cyan-300">₹{selectedRoom.price.toLocaleString("en-IN")}</b></div><p className="mt-2 text-xs text-white/60">Final child policy, hotel availability and peak-date supplements are subject to confirmation.</p></div>}<Field label="Special Request"><textarea rows={4} value={message} onChange={e=>setMessage(e.target.value)} className="input"/></Field><div className="rounded-xl border border-slate-200 bg-slate-50 p-4"><label className="flex items-start gap-3"><input required type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)} className="mt-1 h-4 w-4 accent-blue-700"/><span className="text-sm leading-6 text-slate-700">I have read and agree to the <Link href="/terms-and-conditions" target="_blank" className="font-bold text-blue-700 underline">Terms & Conditions</Link> and <Link href="/cancellation-policy" target="_blank" className="font-bold text-blue-700 underline">Cancellation Policy</Link>.</span></label></div><button type="submit" className={`w-full rounded-xl py-4 text-lg font-bold text-white ${festival||enquiryOnly?"bg-orange-600":"bg-cyan-600"}`}>{festival||enquiryOnly?"Send Enquiry":"Book Now"}</button></form><style jsx>{`.input{width:100%;border:1px solid #cbd5e1;border-radius:.75rem;padding:.75rem;outline:none}.input:focus{border-color:#0891b2;box-shadow:0 0 0 2px rgba(8,145,178,.1)}`}</style></section>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><label className="mb-2 block font-semibold text-slate-800">{label}</label>{children}</div>}
