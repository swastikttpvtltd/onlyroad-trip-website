import type { Metadata } from "next";
export const metadata: Metadata = { title: "Goa Tour Packages | Only Road Trip", description: "Explore Goa tour packages for holidays, honeymoons, beaches and corporate offsites with Only Road Trip.", alternates: { canonical: "https://www.onlyroadtrip.com/packages/goa" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
