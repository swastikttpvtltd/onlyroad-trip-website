import type { Metadata } from "next";
export const metadata: Metadata = { title: "Assam Tour Packages | Only Road Trip", description: "Discover Assam tour packages with wildlife, Kaziranga and Northeast experiences planned by Only Road Trip.", alternates: { canonical: "https://www.onlyroadtrip.com/packages/assam" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
