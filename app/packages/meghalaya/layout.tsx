import type { Metadata } from "next";
export const metadata: Metadata = { title: "Meghalaya Tour Packages | Only Road Trip", description: "Discover Meghalaya tour packages for Shillong, Cherrapunji and Northeast nature experiences with Only Road Trip.", alternates: { canonical: "https://www.onlyroadtrip.com/packages/meghalaya" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
