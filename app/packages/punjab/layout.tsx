import type { Metadata } from "next";
export const metadata: Metadata = { title: "Punjab Tour Packages | Only Road Trip", description: "Explore Punjab tour packages for Amritsar, Golden Temple, Wagah Border and heritage experiences with Only Road Trip.", alternates: { canonical: "https://www.onlyroadtrip.com/packages/punjab" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
