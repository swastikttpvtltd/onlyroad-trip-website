import type { Metadata } from "next";
export const metadata: Metadata = { title: "West Bengal Tour Packages | Only Road Trip", description: "Explore West Bengal tour packages for Darjeeling, Kalimpong, Sundarbans and cultural holidays with Only Road Trip.", alternates: { canonical: "https://www.onlyroadtrip.com/packages/west-bengal" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
