import type { Metadata } from "next";
export const metadata: Metadata = { title: "Maharashtra Tour Packages | Only Road Trip", description: "Explore Maharashtra tour packages for Mumbai, Lonavala, Shirdi, Nashik and heritage experiences with Only Road Trip.", alternates: { canonical: "https://www.onlyroadtrip.com/packages/maharashtra" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
